# ── Node / package managers ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Build / output ──
dist/
build/
.aws-sam/
*.tsbuildinfo

# ── Variables de entorno (sólo el .example se commitea) ──
.env
.env.local
.env.*.local

# ── Editor / OS ──
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# ── Coverage / logs ──
coverage/
*.log

# ── SAM / AWS ──
samconfig.toml.local
packaged.yaml
